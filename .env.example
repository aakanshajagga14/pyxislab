# OpenAI — required for AI coaching summaries on /report/[sessionId]
# Get a key at https://platform.openai.com/api-keys
# Local: copy to .env.local (gitignored) and fill in the value
# Vercel: Project Settings → Environment Variables → add OPENAI_API_KEY
#         (Production + Preview). Do not commit real keys.
OPENAI_API_KEY=
